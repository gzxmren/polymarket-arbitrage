# 2026-06-06 工作日志

## 📅 日期
2026-06-06 (周六)

---

## 🎯 本次主题
用户指令"处理已知的数据腐化问题（whales 表 98% 垃圾、DB 47% 死空间等）"。
**核查后发现：腐化大部分早已被修复，记忆/文档过时。** 故本次重心转为
**核实现状 → 修残留小 bug → 更正记录(CLAUDE.md / PROJECT_REVIEW / 记忆) → 写日志**。
用户明确范围：修残留小 bug + 更正记录 + 代码 review 后完善 + 同步工作日志（**不**删空行、**不**做 TTL 轮转——未选）。

---

## 🔍 核查结论（对 live DB 实测，非文档推断）

权威工具：`python3 scripts/data_health_check.py` → **垃圾率 0.0% / 死空间 0.5% / has_activity 错标 0**。

| 评审清单(2026-06) | 实测现状(2026-06-06) | 判定 |
|---|---|---|
| whales 97.9% 垃圾 | 纯空行 **仅 1 行**/3035；其余 1394 行是有真实成交量/笔数的 trade-flow 鲸鱼 | ✅ 已修，**且原判据错** |
| DB 47% 死空间 | freelist **0.5%**(168/12584 页)，51MB(曾 138MB) | ✅ 已 VACUUM |
| has_activity set-once bug | 主路径 `data_sync.py:188` 已改直接赋值(能归 0) | ✅ 已修 |
| Phase-3 七张表空 | 仍全空(signals/whale_performance/...) | ⚠️ 仍开放(功能没接调度,非腐化) |
| DBTables.SIGNALS 名称漂移 | 整个 DBTables 类**全库 0 引用**(死常量) | ⚪ 无害,已顺手修对 |
| 无文件轮转 | 07-data 286MB/~2200 文件；positions_archive 38601 行(3.4×) | ⚠️ 仍开放(磁盘卫生) |

**关键认知（务必记住，别再犯）**：评审里"whales 98% 垃圾"的判据 `total_value=0 AND position_count=0`
**本身是错的**——它会把"已平仓/无当前持仓快照、但 changes 表里有真实成交流水"的 trade-flow 鲸鱼
误判成垃圾。这类鲸鱼正是"跟鲸鱼"策略的信号源，**不能删**。真·空壳的正确判据是再加
`changes_count=0 AND total_volume=0`（`scripts/cleanup_whales.py` 早在 6-03 就用对了）。
今天差点照旧判据删 1395 行，其中 1394 行是真数据——先核查救了一命。

---

## 📝 代码改动（全为注释/常量，零运行行为变更 → dashboard 服务**无需重启**）

1. **`06-tools/analysis/config.py`** — `DBTables`：`SIGNALS` 由 `'semantic_signals'` 改为真实表
   `'signals'`，新增 `SEMANTIC_SIGNALS='semantic_signals'`，加注释说明该类当前 0 引用、为预留常量。
   （解决评审点名的"名称漂移"；0 引用 → 零风险）
2. **`dashboard/backend/app/services/data_sync.py`** —
   - 价值闸门(`[P0-2]`,~137 行)注释改准确：原称"与 cleanup_whales 判据一致"是误导，实则此闸门只拦
     whale_states 来源的新空行，trade-flow 鲸鱼走 sync_changes、不经此路径。
   - ON CONFLICT(~172 行)上方加"防误改"语义说明：`has_activity/total_value/position_count`=直接赋值(能归零)；
     `total_pnl/changes_count/total_volume`="保优"语义(仅传入>0 才覆盖)是有意为之，别当 bug 改。
3. **`06-tools/monitoring/sync_changes.py`** — `has_activity=1` 硬编码处加注释：本循环只处理"本批确有成交"
   的钱包，标 1 语义正确，非 set-once bug；这些是 trade-flow 鲸鱼，cleanup 明确判为非垃圾。

验证：`py_compile` 三文件通过；`DBTables.SIGNALS=signals / SEMANTIC_SIGNALS=semantic_signals` 自检正常。

## 📝 文档/记录更正
- **`CLAUDE.md`** "Known data-integrity issues" 整段重写为 RESOLVED / STILL OPEN 两栏，标注复核数字与权威检查命令。
- **`PROJECT_REVIEW.md`** 顶部加 🟩 2026-06-06 复核更正说明（原文保留作历史）。
- **Claude 个人记忆**（`~/.claude/.../memory/`）：更新 review-findings / improvement-plan，新增本次数据健康复核结论。

---

## ⏭️ 仍开放（本次按用户范围**未做**，留给后续）
1. **删那 1 行纯空行 + 完整性校验**（`cleanup_whales.py --apply`，安全，1 分钟）。
2. **TTL 文件/归档轮转**：07-data 文件堆积、positions_archive 38601 行无保留期。需定保留天数。
3. **Phase-3 空表**：接上调度链才会有数据（属功能完成，非腐化修复）。

---

## 🧭 盈利方向决策（同日,数据腐化之后的更大议题）

用户追问"除了数据腐化,整个项目的修复进展?目标是实盘盈利"。给出诚实评估并落档:

**三支柱判断**:① 扛得住真实资金的盈利策略=❌**没有**(真拦路虎) · ② 下单/执行层=❌**不存在**(trading 只有 README) · ③ 干净数据底座=✅够用(但从来不是瓶颈)。
**关键数字**(`selective_whale_20260605` +1d):等权 +52.4% 但**资金加权 −0.85%/每元** → edge 在薄市场+彩票分布,放大即亏。三策略(跟鲸鱼/跨市场/精选)全部**未过资金加权测试**。
**核心认知**:修数据/bug/工程债**不会让盈利数字由负转正**;前提是先有"资金加权为正"的策略。

**用户选定方向**:**"2 为台、1 为目标"**(在路2 微仓实盘基础上推进路1 找 edge)。我的回答=**能且推荐,但带 3 前置条件**:
①把路2 定位成"测量仪器"非盈利来源;②路1 先离线产出 ≥1 个"资金加权 ≥0"候选,再让路2 实盘验证;③执行层从"最小封顶+熔断"建起,每阶段卡 Go/No-Go。
分阶段:P0 离线攻坚资金加权候选 → P1 影子盘 → P2 微仓实盘(需授权) → P3 谨慎放大。**红线**:先设资金上限/最大亏损+建 kill-switch 再下单;私钥绝不入库。

**产物**:
- `docs/DECISION_PROFITABILITY_2026-06-06.md` + `docs/reports/profitability-decision_20260606.html`。
- **下一步等用户拍板**:是否从 P0(纯离线、零资金风险、攻坚资金加权候选)开始。涉及真钱/私钥的 P2 起必须先获明确授权。

---

## 🔬 P0 攻坚执行结果（同日,决策后立即开干）

用户拍板"从 P0 开始:看能否打磨出 OOS 资金加权≥0 候选"。已完成,**结论 🔴 没找到**。

**新增脚本**:
- `08-backtests/diagnose_cap_weighted.py` — 诊断资金加权漏点(只读,分桶+相关性)。
- `08-backtests/run_pzero_oos.py` — 预登记假设 + train/test 切分 + 抗单点 + 容量前沿的 OOS 攻坚。

**P0-A 诊断**:全量 +1d 等权 +5.91% 但资金加权 −0.40%。漏点=钱集中在"大额单(>$1k 占 64.6%,−2.16%)×超流动(>$200k 占 61.8%,−1.82%)×长周期×准定局"——即最有效、鲸鱼最无信息优势的市场。等权 +52% 全在铺不开钱的薄市场。

**P0-B OOS 攻坚**(6 假设 × 2 持有期,3 个 cutoff 复核):
- **每个过滤都 train 正→test 负**(H5:+21.6%→−6.75%),教科书式过拟合/无持续 edge。
- 36 次检验(3 cutoff×12 组合)只 1 个边际为正(+0.43%),且不跨持有期/切分稳健 → 纯噪声。
- 容量前沿(事后,不预测):H5 每笔≤$500、整段总~$8k 才勉强不亏。

**判定**:决策文档要求"P0 先产出 ≥1 个 OOS 资金加权≥0 候选才上路2"——现=**0 个**。**此刻投真钱=赌一个回测已知不持续的东西,不做。** 这是省钱的结论(零成本挡住一次必亏实盘)。

**产物**:`docs/P0_CAPWEIGHTED_RESULT_2026-06-06.md` + `08-backtests/results/pzero_oos_20260606_171458.json`。

**下一步(等用户拍板,我建议先1+保持2开放)**:① 攒数据 1~3 月后用同套脚本复跑再盖棺;② 换信号源/假设,走同一"回测→OOS→资金加权"纪律;③ 暂停盈利线。**拿到 OOS 资金加权稳健≥0 候选前不碰真钱。**

---

## 🧠 给 OpenClaw / 下个会话的一句话
数据腐化这条线**基本收尾**：DB 实测健康(0% 垃圾/0.5% 死空间)，今天只做了注释级修整 + 更正过时记录。
别再以"DB 98% 脏"为前提束手束脚；要清垃圾用 `cleanup_whales.py`（判据已正确），要体检用 `data_health_check.py`。
**更大的事**:实盘盈利的真瓶颈是"没有资金加权为正的策略",已落决策文档,方向定为"2 为台、1 为目标"。
**P0 已执行完毕,结论 🔴**:跟鲸鱼无 OOS 资金加权 edge(train 正/test 负,过拟合),未达上实盘门槛 → **不碰真钱**。
脚本 `08-backtests/run_pzero_oos.py` 可在攒更多数据后复跑。下一步建议:先攒数据复验 + 保持换信号源开放。
