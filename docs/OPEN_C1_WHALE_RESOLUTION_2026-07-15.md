# 立项(未预登记 · 未调查):跟鲸鱼 + 持有到**真实结算**,为何变正?

**状态**: 🟢 **已预登记(2026-07-21)→ `docs/PREREG_C1_WHALE_RESOLUTION_2026-07-21.md`。当前卡在前置工程:须先给 `backfill_price_history_api.py` 加 `--universe whales` 回填 H6 原生宇宙的官方结算,方可开跑。**
**来源**: `docs/PREREG_H6_TRUTH_EXIT_2026-07-15.md` 测试 C1 的意外结果。
**处置**: 预登记已完成(见上)。**在前置回填完成、判决落表前,不得基于本文件的 +34% 下任何结论,更不得据此碰真钱** —— 那张表跑在错误宇宙(校准候选池),仅作动机,不作数。

---

## 1. 现象

C1 把 `resolution` 口径的了结价从"我们最后一次拍到的快照价"换成**官方真实结算**(1.0/0.0),其余全同:

| 假设 | 最后快照(arm A) | 官方结算(arm B) | n |
|---|---|---|---|
| H6 `H5+微仓$200` | +2.81% / +1.49% | **+34.06% / +15.66%** | 605 |
| H7 `H6+价格下限≥0.20` | +3.62% / +2.47% | **+10.92% / +7.94%** | 424 |

(cutoff 2026-06-22;4 个 cutoff 全过。cw / 剔顶。)

**方向与预判相反**:原以为"最后快照"会**抬高**收益(如同校准候选那样),实测它一直在**压低** H6。

## 2. 为什么这不是好消息,而是一个"更需要被怀疑"的对象

按 2026-07-15 确立的铁律(见记忆 `truth-source-bias-2026-07-15`):

> 偏差吹不倒本来就趴着的,只可能吹起本来站不住的。**🟢 才是风险区。**

本结果**直接冲突**于既往结论——"跟鲸鱼不赚钱"(净 −2.29%、资金加权 −4~−5%,见 `stratified-findings-2026-06-08`)。**一个推翻既往证伪的 🟢,应受更严审视,而非更松。**

## 3. 已知的、必须先排除的混淆(调查前须逐条处理)

1. **受限宇宙(最大嫌疑)**。C1 只能跑在 9161 个"有官方真值"的市场上,而该池是 `backfill_price_history_api.py --universe candidates` 按**校准**的入场规则(dte 1–14 且有量且 `0.02<px<0.98`)圈定的——**不是 H6 的原生宇宙**。
   - 注意 `0.02<px<0.98` 这一条**排除了在 dte1-14 就已成定局的市场**,这与结果相关性存疑,须专门检验。
   - 对照事实:C1 宇宙内 H2(dte≤14)的 n=1783 已逼近 H0 的 n=1906 → 池子几乎全是短周期,H0/H2 在此几乎同义。
2. **既往结论的口径差**。旧的"跟鲸鱼不赚钱"跑在**全库 + 最后快照**;C1 arm B 是**受限宇宙 + 真实结算**——**两个变量都不同**,故二者**不构成直接矛盾**,不可简单说"平反"。
3. **`resolution` 口径本身不是当前主力**。滚动验证盯的是 `+1d`(`pzero_review_digest.py:110`),而 `+1d` 已由 C2 判 🔴(edge 4.69% < 出场票价 7.7%)。故本项即便成立,也是**另一条策略**(持有到期),不是 H6 的复活。
4. **容量缺口**。持有到期意味着资金占用更久;而官方接口**不提供逐日成交量**,加深历史后的容量代理尚未验证(见 `api-backfill-2026-07-15`)。
5. **胜负分布**。+34% 且剔顶 +15.66% 说明非单点,但须查是否由"低价买入 → 结算 1.0"的少数高倍数交易在**资金加权**下撑起;须做分层与容量前沿。

## 4. 调查前必须完成的预登记(下次开工第一件事)

新开 `PREREG_*.md`,至少钉死:
- **宇宙**:必须跑 H6 的**原生宇宙**。为此须先把回填扩到 whale 信号覆盖的市场(`--universe` 需新增模式),而非复用校准候选池。
- **自变量**:有且仅有一个(建议:同一宇宙下 `最后快照` vs `官方结算`)。
- **红线**:逐字沿用 `run_pzero_oos`(n≥40 且 test 资金加权≥0 且剔顶≥0),`import` 而非复制。
- **假设集**:H0–H7 全列不择优;多重比较照实披露。
- **闸门**:沿用下标对齐闸门(≥95%)。
- **诚实声明**:**本人已看过上表数字**,故对"同一测试"而言不存在纯净事前登记;新预登记只能对**新宇宙下的新结果**成立,须在单中写明。

## 5. 今日不碰的理由(记录决策,非托词)

2026-07-15 一天内已连续证伪两个候选(校准 edge、H6/H7 `+1d`)。**此刻最容易看见自己想看见的东西。** 用户与我共同决定:今日只立项,不调查。
